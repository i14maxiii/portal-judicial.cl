<%- include('../partials/header') %>

<div class="max-w-7xl mx-auto px-4 py-10">
    <div class="mb-8">
        <span class="bg-purple-100 text-purple-700 px-3 py-1 rounded-full text-xs font-bold uppercase tracking-wide">Zona Restringida</span>
        <h2 class="text-3xl font-bold text-slate-800 mt-2">Administración del Sistema (Staff)</h2>
        <p class="text-slate-500">Gestión de usuarios, permisos y auditoría.</p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <!-- Card Placeholder -->
        <div class="bg-white p-6 rounded-xl shadow border border-slate-200">
            <div class="w-12 h-12 bg-purple-100 text-purple-600 rounded-lg flex items-center justify-center mb-4">
                <i class="ph-bold ph-users-three text-2xl"></i>
            </div>
            <h3 class="font-bold text-slate-800">Gestión de Usuarios</h3>
            <p class="text-sm text-slate-500 mt-1">Administrar roles y accesos de funcionarios.</p>
            <button class="mt-4 w-full py-2 border border-slate-200 rounded text-sm font-bold text-slate-600 hover:bg-slate-50">Gestionar</button>
        </div>
    </div>
</div>

<%- include('../partials/footer') %>